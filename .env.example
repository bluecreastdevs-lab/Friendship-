# Environment variables
GEMINI_API_KEY=

# Optional: External backend server URL when deploying frontend on Netlify / Vercel
# (e.g. https://your-backend.onrender.com). Leave empty for unified local / Docker / Cloud Run.
VITE_BACKEND_URL=
